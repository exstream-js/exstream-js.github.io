# Let the writer set the pace

Both branches are reliable forks. The source can advance only as quickly as the slowest destination can accept its next record.

Start with different writer speeds, then move the sliders while the flow is running. The fast branch waits for the slow branch rather than allowing an unbounded queue to grow.

The orange edge shows where demand is currently blocked.
